services:
  # PostgreSQL Database
  postgres:
    image: postgres:16-alpine
    container_name: naiera-support-postgres
    environment:
      POSTGRES_USER: naiera
      POSTGRES_PASSWORD: naiera123
      POSTGRES_DB: naiera_support
    ports:
      - "5434:5432"
    volumes:
      - postgres_data_support:/var/lib/postgresql/data
    restart: unless-stopped
    networks:
      - naiera-support-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U naiera"]
      interval: 10s
      timeout: 5s
      retries: 5

  # MinIO (S3-compatible storage)
  minio:
    image: minio/minio:latest
    container_name: naiera-support-minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin123
    ports:
      - "9102:9000"
      - "9103:9001"
    volumes:
      - minio_data_support:/data
    restart: unless-stopped
    networks:
      - naiera-support-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3

networks:
  naiera-support-network:
    driver: bridge

volumes:
  postgres_data_support:
  minio_data_support:
